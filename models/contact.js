class Contact {
  constructor(localId, firstName, lastName, phoneNumbers) {
    this.localId = localId;
    this.firstName = firstName;
    this.lastName = lastName;
    this.phoneNumbers = phoneNumbers;
  }
};

export default Contact;