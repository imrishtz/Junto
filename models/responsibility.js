class Responsibility {
  constructor(id, name, list, responsible) {
    this.id = id;
    this.name = name;
    this.list = list;
    this.responsible = responsible // array
  }
};

export default Responsibility;